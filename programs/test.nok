ADDI $R0 $R1 #10    // move 10 to R1
ADDI $R0 $R2 #6     // move 6 to R2
ADDR $R1 $R2 $R3    // add R1 + R2 to R3
ADDR $R4 $R3 $R4    // add R3 to R4, store in R4
SRIW $R0 $R3 #160   // store R3 contents to memory #160
BUIN $R0 $R0 #0     // loop to start